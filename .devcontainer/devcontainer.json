{
  "name": "Ubuntu Dev Container",
  "dockerFile": "Dockerfile",
"customizations": {
    "vscode": {
        "settings": {
            "terminal.integrated.shell.linux": "/bin/zsh"
        },
        "extensions": [
            "github.copilot-chat"
        ]
    }
},
  "forwardPorts": [3000, 8080],
  "postCreateCommand": "git config --global --add safe.directory ${containerWorkspaceFolder} && chmod a+x .devcontainer/aliases.sh && bash .devcontainer/aliases.sh",
  "containerUser": "vscode",
  "remoteUser": "vscode"
}
